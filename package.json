{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm check && pnpm build-only && pnpm verify:build && theme-package",
    "build-only": "tsc && vp build",
    "dev": "node scripts/dev.mjs",
    "check": "vp check",
    "fix": "vp check --fix",
    "prepare": "vp config",
    "test:build": "node --test scripts/build.test.mjs",
    "skills:update": "npx skills update",
    "verify:build": "node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "@halo-dev/theme-package-cli": "1.1.1",
    "@halo-dev/vite-plugin-halo-theme": "1.0.3",
    "@types/node": "^24.12.0",
    "parse5": "^8.0.1",
    "typescript": "^6.0.3",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "yaml": "^2.8.3"
  },
  "engines": {
    "node": ">=24.21.0",
    "pnpm": "^12.4.2"
  },
  "packageManager": "pnpm@12.4.2"
}
