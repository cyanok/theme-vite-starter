{
  "type": "module",
  "scripts": {
    "build": "tsc && vp build && theme-package",
    "build-only": "tsc && vp build",
    "dev": "vp build --watch",
    "check": "vp check",
    "fix": "vp check --fix",
    "prepare": "vp config",
    "skills:update": "npx skills update",
    "verify:build": "node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "@halo-dev/theme-package-cli": "1.1.1",
    "@halo-dev/vite-plugin-halo-theme": "1.0.3",
    "@types/node": "^24.12.0",
    "typescript": "^6.0.3",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=24.11.0",
    "pnpm": ">=11.24.0"
  },
  "packageManager": "pnpm@11.24.0"
}
